// starfall-cli config
// https://github.com/cenfun/starfall-cli

export default {
    tempPath: '.temp',

    clean: {
        exclude: ['.env', '.ssh', 'id_rsa']
    },

    outdate: {
        exclude: [

        ]
    }
};
